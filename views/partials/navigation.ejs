<!-- Navigation -->
<nav class="navbar">
    <div class="navbar__inner">
        <div class="brand">🚜 Harvest Harmony - Prototype! 🌾</div>
        <div class="navbar__right">
            <span class="current" id="current-page">Current: Landing</span>
            <button class="search-btn" id="open-search" title="Search Machines">
                <span>🔍</span>
            </button>
            <div class="chips">
                <button class="chip chip--active" data-page="landing">🏠 Landing</button>
                <button class="chip" data-page="farmer">🌾 Farmer</button>
                <button class="chip" data-page="operator">🔧 Operator</button>
            </div>
            <div class="palette">
                <span class="dot dot--green"></span>
                <span class="dot dot--orange"></span>
                <span class="palette__label">Theme Colors</span>
            </div>
        </div>
    </div>
</nav>